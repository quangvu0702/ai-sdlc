# Agent instructions

Read and follow `skills/i-have-adhd/SKILL.md` on every turn until the user says `stop adhd mode` or `normal mode`.

Project skills live in `skills/`. Invoke the SDLC orchestrator with `$sdlc` or `/sdlc`.
