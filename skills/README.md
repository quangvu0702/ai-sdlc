# Shared Agent Skills

This directory is the source of truth for local agent skills.

- `.claude/skills`, `.codex/skills`, and `.cursor/skills` are symlinks to this directory.
- Add or update skills here instead of copying the same folder into each agent config.
- Keep internal skill references relative to the repo root, for example `skills/example-skill/SKILL.md`.

